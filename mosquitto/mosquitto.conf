# Minimal Mosquitto config: allow anonymous, enable websocket listener on 9001
listener 1883
allow_anonymous true

# Websocket listener (useful if frontend uses websocket transport)
listener 9001
protocol websockets

log_dest stdout
